{
  "mcpServers": {
    "youtube": {
      "command": "node",
      "args": ["youtube-mcp/server.mjs"],
      "env": {
        "YOUTUBE_CLIENT_ID": "${YOUTUBE_CLIENT_ID:-}",
        "YOUTUBE_CLIENT_SECRET": "${YOUTUBE_CLIENT_SECRET:-}",
        "YOUTUBE_REFRESH_TOKEN": "${YOUTUBE_REFRESH_TOKEN:-}"
      }
    }
  }
}
